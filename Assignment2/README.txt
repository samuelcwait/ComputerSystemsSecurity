To run python_exploit.py, move the program into the getscore folder.
Then run python_exploit.py by entering the following command:

    python python_exploit.py

once run, the exploit will have hijacked the jmp esp command to grant 
root access to the redhat9 system in the shell. 